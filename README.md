# Windows Server Active Directory Home Lab

A two-machine Active Directory environment built on a Linux workstation: one Windows Server domain controller and one domain-joined Windows client, running as VMs on an isolated network.

The point is not that a domain exists. It is the documentation — the design decisions, the verification steps, and the problems encountered and resolved along the way.
